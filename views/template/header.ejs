<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title></title>
    <link href="<%- baseUrl -%>/bootstrap/css/bootstrap.min.css" type="text/css" rel="stylesheet">
    <link href="<%- baseUrl -%>/stylesheets/header.css" type="text/css" rel="stylesheet">
    <link href="<%- baseUrl -%>/stylesheets/footer.css" type="text/css" rel="stylesheet">
    <link href="<%- baseUrl -%>/stylesheets/screen.css" media="screen, projection" rel="stylesheet" type="text/css" />
    <link href="<%- baseUrl -%>/stylesheets/benefits.css" media="screen, projection" rel="stylesheet" type="text/css" />
    <link href="<%- baseUrl -%>/stylesheets/margin+padding.css" media="screen, projection" rel="stylesheet" type="text/css" />
    <link href="<%- baseUrl -%>/stylesheets/pricing.css" media="screen, projection" rel="stylesheet" type="text/css" />
    <link href="<%- baseUrl -%>/stylesheets/complement.css" media="screen, projection" rel="stylesheet" type="text/css" />
    <link href="<%- baseUrl -%>/stylesheets/slick.css" type="text/css" rel="stylesheet" media="screen, projection">
    <link href="<%- baseUrl -%>/stylesheets/slick-theme.css" type="text/css" rel="stylesheet" media="screen, projection">
    <script src="<%- baseUrl -%>/js/jquery-3.2.1.slim.min.js"></script>
    <script src="<%- baseUrl -%>/js/popper.min.js"></script>
    <script src="<%- baseUrl -%>/js/slick.min.js"></script>
    <script src="<%- baseUrl -%>/js/script.js"></script>
    <script src="<%- baseUrl -%>/bootstrap/js/bootstrap.min.js"></script>
</head>
<body>

    <header class="container-fluid sticky-top">
        <nav class="navbar navbar-header navbar-expand-lg navbar-light">
            
            <a class="navbar-brand" href="/"></a>
            <div class="top-menu">
                <ul>
                    <li><a href="#">VENDOR LOGIN</a></li>
                    <li class="p-left15"><a class="btn btn-primary" href="#">Start Free Trial</a></li>
                    <li><a href="#">
                        <div class="dropdown show">
                            <a class="dropdown-toggle btn" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <%- lang -%>
                            </a>
                            <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                                <% Object.keys(menu).forEach(function(key) { %>
                                    <% if(key == active) { %>
                                        <a href="<%= baseUrl + menu[key].url%>" class="dropdown-item en" href="#">EN</a>
                                        <a href="<%= baseUrl + '/id' + menu[key].url%>" class="dropdown-item id" href="#">ID</a>
                                    <% } %>
                                <% }); %>
                            </div>
                        </div>
                    </a>
                </li>
                </ul>
            </div>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo01" aria-controls="navbarTogglerDemo01" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
            </button>
        </nav>
        <nav class="navbar navbar-expand-lg navbar-light bg-faded">
            
            <div class="collapse navbar-collapse" id="navbarTogglerDemo01">
                <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                    <% Object.keys(menu).forEach(function(key) { %>
                        <% if(key == active) { %>
                            <li class="nav-item">
                                <a class="nav-link active" href="<%= menuUrl + menu[key].url %>"><%= menu[key].name %></a>
                            </li>
                        <% } else { %>
                            <li class="nav-item">
                                <a class="nav-link" href="<%= menuUrl + menu[key].url %>"><%= menu[key].name %></a>
                            </li>
                        <% } %>
                    <% }); %>
                </ul>
            </div>  
        </nav>

    </header>


    